# TASK.md — K3V SHINJUKU GYOEN 公式サイト制作タスク

進め方・デザイン方針は [CLAUDE.md](CLAUDE.md) を参照。要件定義書は Source of Truth: https://docs.google.com/document/d/1CMOUpOBNIdinqRwsINJwuFoK9J3Pr7wXrcku125ysZo/edit

凡例: `[ ]` 未着手 / `[~]` 進行中 / `[x]` 完了

---

## Phase 0 — コンテンツ整合（最優先・他タスクより先に着手）

現行TOPページは要件定義書と異なるブランド設定（施設名・立地・部屋数・世界観）で書かれている。以降のページを増やす前に、または着手時にこの土台を直す。詳細は CLAUDE.md の「既知の不整合」表を参照。

- [x] `messages/ja.json` / `en.json` / `zh.json` を要件定義書のコピー案に合わせて全面更新
  - [x] `meta`: サイト名を K3V SHINJUKU GYOEN に、タグライン "Not Just a Stay. Live Tokyo Beautifully." を反映
  - [x] `hero`: コピー「新宿御苑のすぐそばで、東京を暮らすように滞在する。」（EN: "Stay steps away from Shinjuku Gyoen. Design Apartments in the Heart of Tokyo."）
  - [x] `nav`: ロゴ表記を K3V に統一（`Nav.tsx` のハードコード "K3v" も修正済み）
  - [x] `concept`: ブランドストーリーの核（「暮らすように旅をする」体験、3部屋=3ライフスタイル）に書き換え。「和と北欧」軸は撤去し、UNICO / JOURNAL STANDARD FURNITURE / CRASH GATE の3ピラーに変更
  - [x] `rooms`: ROOM 01 を UNICO ROOM / Natural Living（北欧ナチュラル、木の温もり）に。ROOM 02 (JOURNAL STANDARD FURNITURE / Urban Vintage)・ROOM 03 (CRASH GATE / Industrial Modern、詳細準備中を明記) は現行コピーを流用しつつ要件文面と整合
  - [x] `access`: 谷中/根津 → 新宿御苑前・新宿駅アクセスに全面差し替え。住所は「東京都新宿区（詳細はお問い合わせください）」とプレースホルダー明記（クライアント確認待ち）
  - [x] `footer`: 部屋数「8室」表記を削除、施設名・住所を修正
- [x] `components/` 側のハードコード文言（Nav.tsx / Footer.tsx の "K3v" ロゴを "K3V" に修正。Footer の問い合わせ用メールアドレスも新ブランド名に更新）
- [x] 上記変更後、TOPページをローカルで目視確認（3言語すべて、モバイル幅含む）

---

## Phase 1 — TOPページ ブラッシュアップ（"チープシック"方向への調整）

- [x] 現行デザイン（アーストーン+角丸+Zen Maru Gothic）を「高級感」に寄りすぎていないか確認。ヒーロー画像・モバイルFABの影を少し軽量化する程度の微調整に留め、大幅な作り直しは不要と判断
- [ ] ファーストビュー: 要件定義書 7章の動画演出（新宿御苑→朝の街→カフェ→地下鉄→マンション外観→部屋→家具→コーヒー→夜景）を将来的に動画化する前提。現状は静止画イラスト（`HeroIllustration.tsx`）のまま — 動画化はクライアントから素材が揃うタイミングで着手
- [x] ヘッダーに言語切替（ja/en/zh）を常時表示 — `Nav.tsx` が `hidden lg:block` でモバイル非表示になっており、`LocaleSwitcher` がモバイルで一切見えないバグを発見・修正。常時表示のコンパクトヘッダー（ロゴ＋言語切替）＋lg以上でフルナビに拡張する構成に変更
- [x] フローティング予約ボタン（モバイルでスクロール時に画面下部固定の「空室検索/Book Now」）— `MobileBottomNav.tsx` で実装済み・常時1クリックで到達可能なことを確認。ただし実際の外部予約エンジン連携はPhase 3待ちのため、現状はお問い合わせ導線止まり（3クリック予約の完全達成はPhase 3で再確認）
- [x] スクロール構成を要件定義書 7章の9セクション順に合わせて拡張（`app/[locale]/page.tsx`）:
  1. Why K3V? — 実装済み(`Concept.tsx`)
  2. Prime Location — **新規実装** (`PrimeLocation.tsx`)。徒歩分数（御苑6分/丸ノ内線3分/新宿駅18分）・周辺コンビニ等を掲載
  3. Newly Renovated — **新規実装** (`Renovated.tsx`)
  4. Everything You Need — **新規実装** (`Amenities.tsx`)。アイコン8種＋「ほか完備」注記で簡潔に構成
  5. ROOMS — 実装済み(`Rooms.tsx`)
  6. Discover Tokyo — `LivingMoments.tsx` を要件定義書の実際のモデルコース（新宿御苑→神楽坂→渋谷Blue Bottle→渋谷スカイ→夜）に差し替え
  7. Guest Reviews（3・8を統合）— **新規実装** (`Reviews.tsx`)。⚠️ 開業したばかりでレビュー実績が無いため、捏造レビューは掲載せず「準備中」プレースホルダーとして実装。**クライアントから実際のレビューが届き次第、差し替え必須**
  8. Why Book Direct? — **新規実装** (`BookDirect.tsx`)。Best Rate/柔軟キャンセル/ローカルガイドPDF/アーリーレイト/優先サポート → Book Now

  実装順は Hero → Concept → PrimeLocation → Renovated → Amenities → Rooms → Moments(DiscoverTokyo) → Reviews → BookDirect → Access(実務情報+マップ) → Footer。既存の `Access.tsx`（住所・最寄駅・チェックイン時間・マップ）は要件定義書の9セクションには無いが、実務情報としてFooter直前に維持。

---

## Phase 1.5 — クライアントfeedback反映（2026-08-17 スタディ結果）

デザイン7案のうち **washed chambray** と **gyoen green** の2案に絞り込み。この2案を次回レビューで決着させる。
判断基準は CLAUDE.md の「チープシック」方針（重い・高級・クールに寄せない）。

**保全**: denim / timber / kraft / doma の4案は一切変更しない（`git diff` で無変更を確認する）。`app/mock-structure.css` は該当デザインのスコープ内のみ編集する（過去に他案へ波及した事故あり）。

### A. 共通コンテンツ追加（chambray / gyoen 両方）

実装: `mockShared.tsx` の `MockConvenience` / `MockFacilities`。文言は `primeLocation.convenience` / `amenities.facilities` を**追加のみ**（既存キー無変更なので他4案の出力は不変）。

- [x] **立地の「利便性」訴求を強化** — PrimeLocation の下に追加
  - [x] 至近にこれだけ揃っている、という一覧性（コンビニ / スーパー / 飲食 / ドラッグストア等）
  - [x] 深夜でも買い物に出られる
  - [x] 新宿三丁目はすぐ、歌舞伎町へも歩ける
  - [x] コピーは畏まった敬語を避け、暮らしに寄り添うカジュアルな語り口
  - ⚠️ 新宿三丁目・歌舞伎町の**徒歩分数は要件定義書に記載がないため書いていない**（「すぐそこ」「歩ける」の定性表現に留めた）。実測値が出たら追記する
- [x] **部屋別・全室共通の設備訴求を追加** — 既存 Amenities 内に収めた（セクションは増やしていない）
  - [x] 305 / 401: ベンチ付きシャワーユニット（2名でも使える）、小上がり、床下スーツケース収納
  - [x] 503: 和バス
  - [x] 305: トイレ2箇所
  - [x] 全室: シャワートイレ / Wi-Fi / ミュージック仕様（機器未選定のため「音楽を流せるオーディオ」表記）
  - ⚠️ 部屋番号 305/401/503 とブランド3室の対応は未確定のまま。**番号のみ表示**し、紐付けは `messages/*.json` の `amenities.facilities.rooms[].room` 1箇所に集約済み（確定したらそこだけ直す）

### B. washed chambray の個別改修

- [x] **近隣SHOPは横スライドを維持**（timber-indigo の4コマグリッドへの作り替えは不採用）。**オートプレイ版**を別バリエーションとして追加
  - [x] オートプレイは低速・等速、ホバー/タッチ/フォーカス中とタブ非表示中は停止、`prefers-reduced-motion` で無効化
  - [x] カード列を必要数だけ複製して継ぎ目なくループ（複製は `aria-hidden`）
  - [x] 写真（Scene）が主役として大きく見える構成を維持
  - [x] **2026-08-21 決定：オートディスプレイで統一。** A/B（手動 / オートプレイ）の比較は終了し、切替属性 `data-wc-rail` と配色プレビューの「近隣SHOPの見せ方」項目を撤去。実装は `app/[locale]/_components/chambray/autoRail.ts` に一本化し、**Chambray Gyoen も同じオートディスプレイ**に揃えた（両デザインで scroll-snap を解除・スクロールバー非表示）
- [x] **ウッドの色柄を全デザイン共通に統一**（2026-08-21・クライアント提供の参考写真＝斜め木目のあたたかいオレンジ寄り無垢材）
  - [x] 統一前はサイト内に4種のウッドが同居していた：washed-chambray `#6B4A32`（ウォルナット）／ chambray-gyoen `#C9A26E`（ペールオーク）／ footer 案2 `#C89055`（ハニーオーク）／ gyoen-green `#7a3f1f`・`#3e251a`（ローズウッド・ウォルナット）
  - [x] 単一の出どころを `app/wood.css` に新設（`globals.css` で3枚のデザインシートより後に import）。各デザインシートの `--wood*` は値を持たず、コメントでここを指すだけにした
  - [x] 色: `--wood #CC8043` / `--wood-lt #E0A063` / `--wood-dk #AC5F27` / `--wood-deep #8C481E` / `--wood-pale #F2DCC2` / 明るい地の文字用 `--wood-ink #84461B`
  - [x] 柄: 参考写真どおり木目を**斜め（-34°）**に。feTurbulence を回転させた1枚を共通化し、縦板は角度だけ変える。板の継ぎ目 `--wood-seam` も共通
  - [x] gyoen-green の `<use href="#wood-rose|#wood-walnut">` の板イラストは停止（SVGとCSSで木目が2系統に割れるため）。symbol と `gyoen/defs.ts` は 2026-08-22 のイラスト統一で削除済み
  - [x] **木の上の文字は濃いインクに反転**。中間色になったのでクリームは乗らない（`--ecru` で 2.8:1、ペールオーク時代の `.rail` は 2.1:1 だった）。共通の `--wood-on #2b2620` で 4.5:1 以上（面のグラデーションは 6.69〜4.54:1 に収まる帯に制限）
  - ⚠️ chambray-gyoen は「濃い黒は使わない」設計だが、木の上の小さなラベルだけ例外にした。あのデザインの最濃インク `#3F5261` はこの材の上で 2.6:1 しかなく、面を明るくすると参考写真から離れるため
  - [x] 材名のコピーも1つに: `gyoen.concept.swatchWood` を `OAK / ROSEWOOD` → `WOOD`、`chambray.footer.creditGyoen` の `Oak` → `Wood`（ja/en/zh）
  - [x] `docs/top-page-mock-8.html`（washed chambray のモック原本）も同じ色柄に追従。SVG は無改変なので `scenes.ts` の生成結果は変わらない（15点のまま）
  - ⚠️ gyoen-green の ROOM 02 は 900px 以下でコピーがウッドパネルの上に重なる**既存のレイアウト不具合**あり。統一前（濃いローズウッド＋濃緑の文字で約1.3:1）より読めるようにはなったが依然 4.5:1 未満。ウッドの問題ではなくレイアウトの問題なので別途
- [x] **最終セクションの濃茶ブロックを差し替え**。3案とも実装し切替比較できる
  - [x] 案1 `plaster`: 素材感のある塗り壁 `#EFEAE0`（既定）
  - [x] 案2 `wood`: ウッド柄（ハニーオーク `#C89055` → **2026-08-21 の色柄統一で共通ウッドに吸収**）
  - [x] 案3 `moquette`: リネン織りのセージ `#8FB68C`（同上）
  - [x] **2026-08-21 決定：素材（ウッド・緑）をサイト全体で統一。** クライアント指示の参考写真2点（あたたかいオレンジの無垢材／セージの織柄クロス）を正とし、値は `app/wood.css` と `app/green.css` の2枚に集約した。各デザインシートは色を持たず、この2枚を参照するだけになっている（globals.css で **mock-structure.css より後**に import すること）
    - [x] ウッド：`#6B4A32`（ウォルナット）/ `#C9A26E`（ペールオーク）/ `#C89055`（ハニーオーク）の3系統を `#CC8043` の1系統へ。木目は参考写真に合わせて斜め（-34°）に統一
    - [x] 緑：`#33502F` 系 / `#4C6E31` 系 / `#83A94D` 系 / `#8FB68C` を、参考写真のセージ `#7BAA75` から起こした1本のスケールへ
    - [x] **中間色の地には濃い文字**という規則に統一。ウッドの上・クロスの上のクリーム文字はすべて反転させ、階層は不透明度ではなく太さと字送りで作る
  - [x] **gyoen-green を案Aで実装**（クライアント指摘「構成・レイアウトは大変気に入っているが、暗い・深い緑が高級感を演出してしまってダメ」）
    - [x] **レイアウトには一切触っていない。** 変更したのは色だけで、`gyoen-design.css` の構造・グリッド・タイポの指定は無変更
    - [x] ヒーロー／ナビ／ROOM 03／LOCATION／CTA／フッターの濃緑の面をセージの織柄クロスに置き換え、乗っていたクリームのセリフ体を黒の太字へ（クロス上で 6.8:1）
    - [x] ヒーローは樹冠イラストを一切変更せず、上のスクリム（`#1E2A1C` の 0.86）を明るいセージのヴェールに差し替えるだけで明るくした。奥側も 0.62 までしか抜かない — 0.30 だと樹冠の最暗部 `#1F3A2E` が出て、指摘された深緑が画面右上に残る
    - [x] ボタンだけがページ内で唯一の濃い緑になり、押せるものが際立つ構成に
    - [x] `GyoenTop.tsx` のインラインスタイル（`var(--green)` / `var(--sky)` を直接指定していた9箇所）も新しい役割トークンへ差し替え
  - ⚠️ **テーマ名「Gyoen Green」の再検討が要る**。深い御苑の緑ではなくなったため
  - [x] **イラストを chambray-gyoen に統一**（2026-08-22 クライアント指示）。上の「イラストは未変更」の申し送りはこれで解消
    - [x] `_components/gyoen/defs.ts` を削除。このデザイン固有の抽象シーン（`#sc-canopy` / `#sc-room-a|b|c` / `#sc-entry` / `#sc-coffee` / `#sc-lawn` / `#sc-street` / `#sc-map`）は全廃
    - [x] 差し替え先はすべて chambray-gyoen と同じもの — 室内・近所の描き起こしは `../chambray/scenes.ts`、ヒーローの樹冠は `../chambray-gyoen/defs.ts` の `#cg-canopy`。id は描画時に `gy-` へ付け替え（4デザインが同時に DOM にいるため）
    - [x] 割り当て: ヒーロー＝`#gy-canopy` ／ LOCATION の図版＝`LIVING`（縦絵なので 4:3 に slice）／ RENOVATED の図版＝`HERO` ／ ROOM 01–03＝`ROOM1`・`ROOM2`・`ROOM3`
    - [x] ROOM 01 は他2デザインと同じ「確定レイアウト／朝の光」の2枚スライダー（`ROOM1` + `ROOM1_MORNING`、ラジオ + `:has()` のみで JS なし）
    - [x] wood.css で既に無効化されていた `<use href="#wood-rose|#wood-walnut">` の板イラストもマークアップごと撤去
    - [x] 検証: build・lint 通過（エラー0／警告は既存8件）、注入した6シーンとも dangling な id 参照0、1280px・375px とも横スクロール0
  - ⚠️ 今回の変更とは無関係の既存コントラスト不足が残っている（`--sky-deep #58809e` の小ラベル 3.3–4.0:1、washed-chambray の `--thread` カウンタ 2.3:1、`.card p` の 68% アルファ 4.4:1）。別途対応が要る
  - [x] 白の細字を撤去。透明度で階層を作るのをやめ、地を明るく・文字を濃いインクに反転。本文コントラスト 12.5 / 5.41 / 6.59

### C. gyoen green の個別改修

- [x] 構図・立体感はそのまま、ヒーローを切り取るウッドの見切り（`.ledge`）の面のみ胡桃 `#3e251a` → ハニーオーク `#C89055`。徒歩分数キャプションは明るい地に合わせて濃いインクへ反転（クリームのままだと約1.9:1）

### D. バリエーションの比較環境

- [x] 配色プレビューのパネルに washed-chambray 選択時のみ2項目を追加。`?wcRail=` / `?wcFooter=` としてURLに載るので、その状態のリンクをそのまま共有できる
- [x] 切替方式と手順を `docs/design-mocks.md` に記載

### 完了条件

- [x] `npm run build` / `npm run lint` が通る（エラー0、warningは既存8件のまま）
- [x] chambray / gyoen とも ja / en / zh の3言語で表示崩れなし
- [x] 390px 幅で横スクロール（overflow）が発生しない（`document.scrollWidth === 390` を確認）
- [x] denim / timber / kraft / doma の4案が `git diff` 上で無変更（DOM上も新ブロック0件・footer配色不変を確認）
- [x] **オートプレイの実走行を実ブラウザで確認済み**（2026-08-17 / 実Chrome・前面タブ・60fps・dpr2）
  - 検証環境のブラウザペインは `visibilityState: "hidden"` のままで rAF が一切発火しないため確認不能だった。実Chromeのタブを前面に出して計測（macOSのウィンドウ遮蔽で hidden に戻るので、ページ側に計測を仕込んでから Chrome を前面に固定して読み出す方式）
  - 速度 22.0px/s（0.367px/frame・240フレーム実測）／折り返し 971→11.5px で継ぎ目のカード順が連続（`コンビニ→スーパー→カフェ→ドラッグストア`）
  - 停止条件すべて実測: ホバー中 0px / フォーカス中 0px / ホイール操作中は掴んだ位置(600px)を保持し約1.2秒後に自走再開 / `manual` 切替で複製4枚を撤去・`scrollLeft` 0 に復帰し停止 / `auto` 復帰で再複製・再走行
  - 375px 幅でも1周分がスクロール範囲に収まる（loop 968 ≤ maxScroll 1583）、横スクロール 0
  - `prefers-reduced-motion` の無効化は `startAuto` 冒頭の early return によるもので、コード上で確認（OS設定を変える実行時検証は未実施）
- [x] **オートプレイの速度バグを修正**（実走行確認で発覚・`ChambrayBehaviour.tsx`）
  - 毎フレーム `rail.scrollLeft` を読み戻して 0.36px 加算していたため、`scrollLeft` のデバイスピクセル量子化（dpr2 で 0.5px刻み）で端数が毎回切り上がり、実測 0.5px/frame = 30px/s（意図した22px/sより約39%速い）だった。オフセットをfloatで保持する形に変更
  - あわせてフレーム間隔でスケールするようにした（従来は60fps固定前提のため120Hzディスプレイで2倍速になる）。長時間フレームで飛ばないよう差分は50msで上限
  - float保持にした副作用として、`pointerdown` を伴わないホイール/トラックパッド操作が自走に上書きされる回帰が出たため、ホイール/タッチに時限付きの停止を追加し、外部から動かされた場合はその位置を引き継ぐようにした

---

## Phase 1.6 — 不採用モックの資産化（今後のプロジェクト用）

今回不採用となった **denim-sakura / timber-indigo / kraft-riso / doma-ceramic** の4案は、他プロジェクトで再利用できる形で本リポジトリから切り離して保存する。本プロジェクトのコードベースからは最終的に除去し、TOPページの肥大化・ビルド対象の無駄を解消する。

**保存先**: `~/workbench/design-mock-library`（独立Gitリポジトリ・2026-08-17 作成）

- [x] 保存先を決定する
- [x] 4案それぞれについて、再利用に必要な一式を抽出する
  - [x] `app/{denim,timber,kraft,doma}-design.css` → `<slug>/design.css`
  - [x] `app/[locale]/_components/{denim,timber,kraft,doma}/*` → `<slug>/components/`
  - [x] `app/mock-structure.css` の該当スコープブロック → `<slug>/structure.css`
  - [x] `messages/*.json` の該当デザイン用キー（3言語）→ `<slug>/messages/`
  - [x] `docs/gen-*-assets.py` / `docs/add-*-messages.py` → `<slug>/gen-assets.py` / `add-messages.py`
  - [x] 元モックHTML（`docs/top-page-mock-{6,7,9,10}.html`）→ `<slug>/source-mock.html`
  - [x] 6案共通の `mockShared.tsx` と共通ネームスペースのメッセージ（hero / primeLocation / renovated / amenities / rooms / access / mockNav）→ `shared/`
- [x] 各案に README（コンセプト・配色トークン・使用フォント・Google Fontsクエリ・配置先・組み込み手順）を添える
- [x] 切り出し完了後、本リポジトリから4案を除去する（2026-08-17）
  - [x] `app/[locale]/page.tsx` の `DenimTop` / `TimberTop` / `KraftTop` / `DomaTop` の読み込みを削除
  - [x] 上記CSS・コンポーネント・messagesキーを削除
    - `app/{denim,timber,kraft,doma}-design.css` と `app/globals.css` の @import・`[data-theme=...]` パレット定義
    - `app/[locale]/_components/{denim,timber,kraft,doma}/`
    - `app/mock-structure.css` の `.denim-top` / `.timber-top` / `.kraft-top` / `.doma-top` ブロック（2289行 → 1090行）
    - `messages/{ja,en,zh}.json` の `denim` / `timber` / `kraft` / `doma` 名前空間
    - `components/ThemeMock.tsx` のスイッチャー項目（全7段 → 全3段）
    - `app/[locale]/layout.tsx` のフォント読み込み（Archivo / Archivo Black / Anton / Instrument Serif / Zen Maru Gothic 900 を撤去）
    - `docs/top-page-mock-{6,7,9,10}.html` と `docs/{gen,add}-{denim,timber,kraft,doma}-*.py`（すべて design-mock-library に保存済み）
    - `docs/design-mocks.md` を2案構成に更新（撤去した4案の保存先を明記）
  - [x] 削除後に `npm run build` / `npm run lint` が通り、chambray / gyoen のプレビューが従来どおり動くことを確認
    - build 成功（24ページ生成）／ lint エラー0・warning既存8件のまま
    - chambray（ja）・gyoen（en）・current（zh）とも表示崩れなし、コンソールエラー0、390px幅で横スクロール0
    - 撤去済みテーマの旧URL（`?theme=doma-ceramic` 等）は未知IDとして扱われ、保存済みテーマ→`current` にフォールバックする（壊れない）
- [x] 除去は **Phase 1.5 のクライアントレビューで最終案が確定してから**着手する（それまでは比較用に残す）

---

## Phase 1.7 — 統合配色案 chambray-gyoen（green-gyoen × washed-chambray の良い部分だけ統合）

次回意見交換の本命として1案追加。トーンは washed-chambray、ヒーロー構成は green-gyoen（描画方式・レイヤー・構図は流用、色とマテリアルのみ差し替え）。既存2テーマは比較対象として現状維持。

- [x] パレット確定（ベース/サブ/アクセント/テキスト/ボーダー）— 色見本と根拠を報告し承認済み（2026-08-19）
- [x] 実装: `app/chambray-gyoen-design.css` + `app/[locale]/_components/chambray-gyoen/` + `globals.css` トークンブロック + `page.tsx` 併置 + `ThemeMock.tsx` 登録（セクション構成・コピー・i18nキーは washed-chambray 流用。追加キーは署名行 `chambray.footer.creditGyoen` の1つのみ・3言語同時追加）
  - ヒーローは gyoen の `#sc-canopy` / `#wood-walnut` を構造そのまま cg- 接頭辞で複製し色だけ差し替え（`_components/chambray-gyoen/defs.ts`）。イラスト類は `../chambray/scenes` を実行時に `wc-`→`cg-` 変換して流用
  - washed-chambray の暗色3面の置換: 濃紺インク→中明度デニムインク（ROOMSパネル）、温黒アンカー→レンガ（Renovated）、濃茶ウォールナット→明オーク（額縁・レッジ・フッターは明るい塗り壁）
- [x] 完了条件: nav から3テーマ+current 相互切替OK / 本文コントラスト4.5:1以上（最薄の組合せ ink×deep 4.60） / ベース明度 L*94.5→95.3・最暗色 L*15.6→33.7（暗色排除） / ja・en・zh 表示崩れなし・横スクロール0 / build・lint 通過（npm・警告は既存8件のみ）

---

## Phase 1.8 — chambray-gyoen に御苑の植物レイヤーと木漏れ日を追加

「新宿御苑の自然が、Webサイトの中に静かに入り込んでいる」体験を作る。植物を並べるのではなく、**3種に別々の視覚的役割（面・点・線）を割り当てて**密度の破綻を防ぐ設計。

- [x] ヒーローの「地」を織り目から御苑の樹冠に差し替え（統合案）。構図・木枠のイラスト・パッチは現行のまま、背後だけを入れ替え。孤立していた `defs.ts`（樹冠/オークSVG）を復帰
- [x] 植物3種を `_components/chambray-gyoen/flora.ts` に作成
  - ヤツデ＝**面**：掌状葉＋白い球状花序。セクション下部の余白帯から画面端に大株が覗く（2箇所のみ）
  - アオキ＝**点**：光沢のある単葉＋赤い実。ページ唯一の赤で要所を1箇所だけ留める
  - ヨモギ＝**線**：羽状に裂けた葉＋細い穂。`.sidx` の点線ルールの先に接ぎ、全5セクションを同じ文法で繋ぐ
- [x] 追加色は2つだけ：`--berry #C4553A`（L*50.1・レンガ family の中明度、暗色禁止ルール順守）/ `--sage #B9C6A8`（L*78.2）
- [x] 動き：常時の揺れは ±0.7度・11〜16.5秒（位相と周期をずらして同調を防止）。スクロール連動は CSS scroll-driven animation（`animation-timeline: view()`）で `@supports` 内に隔離、非対応ブラウザでは静止した植物として成立
- [x] ヒーローに three.js の木漏れ日レイヤー（`HeroCanopyLight.tsx`）
  - **効果内で `import("three")` を動的実行** → 初期HTMLにも他テーマにも三次元ライブラリは載らない（`grep` で初期HTML参照0を確認）
  - 明るい地では木漏れ日は光ではなく**葉影**として知覚されるため、影優位のシェーダに設計。ドメインワープ＋クラスタ変調で非反復
  - 停止条件：`prefers-reduced-motion` / WebGL不可 / テーマ非選択 / 画面外 / タブ非表示。30fps に間引き、DPR は 1.5 上限
- [x] 検証: build・lint 通過（エラー0／警告は既存8件）、ja・en・zh とも表示、モバイル375pxで横スクロール0、washed-chambray と gyoen-green は無変更

### 動きの較正やり直し（「平坦に見える」フィードバック反映）

初回実装は「気づくか気づかないか」を狙って知覚閾値を下回っていた。実測値: 大株の先端が 8.5px/6.5秒（約1.3px/秒）、スクロール連動は 44px を約3000pxで消化（移動率1.5%）、植物 8個/9000px、木漏れ日はヒーロー限定でページの約1割。以下で作り直した。

- [x] ~~**木漏れ日をページ全体へ**（`CanopyLight.tsx`）。`mix-blend-mode:multiply` の全画面オーバーレイ~~
  → **撤回（2026-08-20 クライアント指摘「失格」）**。全面に乗算の減光を掛ける手法は、
  このデザインの根幹（高明度・暗色を入れない・明るい印象）と真っ向から矛盾する。
  結果として「背景に薄い黒が出た」だけになり、葉の緑まで濁らせ、可読性も全ペアで低下させた。
  コントラスト基準（最小4.59:1）は満たしていたが、**基準を満たすことと良くすることは別**で、
  そこを取り違えたのが判断ミス。`CanopyLight.tsx` と `.komorebi` の CSS は削除済み。
  three.js はどこからも import されなくなったためバンドルから完全に消えた（参照0チャンク）。
  **教訓: 明るさが売りのデザインでは、生気は「足す」ことでしか作れない。減光は必ず逆効果になる。**
- [x] **振幅の較正**: 揺れ ±0.7度 → **±2.6度**（先端の移動 8.5px → 約32px）。スクロール連動は `--drift` で植物ごとに 52〜110px（大株ほど大きく＝手前）。リビールの立ち上がり 24px → 40px・5段
- [x] **コンテンツ自体が動く**: ヒーローの物干しに吊るしたシャツを揺らす（`.cg-hang`／個別プロパティ `rotate` を使用。`transform` だと SVG 側の位置決めを上書きするため）。部屋の帯の写真は通過に合わせて `cg-pan` でパン
- [x] **量を増やす**: 8個 → **23個**、全7セクションへ（ヒーロー・立地・リノベ・設備・部屋・アクセス・フッター）。大株（強）→ 葉1枚（中）→ 一枝（弱）の3段でリズム
- [x] 減光レイヤー撤去後、葉そのものの存在感を上げる方向へ切り替え: 葉・大株・ヨモギ・アオキを一回り拡大（葉 168→215px 上限）し、`opacity` による減光指定をすべて解除して新緑の発色を戻した
- [x] コントラストは減光レイヤー撤去により元の値へ復帰（本文×ページ地 7.20:1 ほか）

### 参考資料4点にもとづく植物の描き直し（2026-08-20）

クライアント提供の写真4点（ヤツデの葉／ヨモギ／アオキの実／斑入りアオキ）を受けて全面的に描き直し。**初回の参考写真は白い花が主役だったが、今回の4点はすべて「葉」**。よって花序ではなく葉の形と明るい緑の面で構成する方針へ転換した。

- [x] **ヤツデの葉脈を葉身より明るい黄緑に反転**（`--vein #CBE38F` > `--leaf #7FB94A`）。写真では葉脈が光っており、ヤツデらしさの大半をこの明暗差が作る。前版は暗い緑の脈で正反対だった。裂片も楕円の集合から先の尖った長い裂片へ
- [x] **斑入りアオキを新規追加**（写真4）。金色の斑 `--variegate #E3CE4B` はページで最も明るい植物要素
- [x] アオキの実を鈍い朱→鮮やかな赤 `#D93B22`、形も球→楕円へ（写真3）
- [x] ヨモギは輪郭1本のパスだと菱形の記号に潰れたため、**中肋に小葉を並べる作り**へ変更（ヤツデと同じ手法）
- [x] 植物専用トークン11色を写真から採色。**全色 L*48 以上**（最暗はアオキの実 L*49.5）で「暗色を入れない」規則を維持
- [x] 配分の是正: ヤツデ8（株3＋葉5）／アオキ系7（実4＋斑入り3）／ヨモギ8（枝3＋見出し脇5）。前版はヤツデ12・アオキ2と偏っていた
- [x] 位置指定を種類非依存のクラス（`he-l` `lo-r` 等）へ整理。置く植物を差し替えても座標が動かない

### 花序（ヤツデの花）の追加と見出しの標の作り直し（2026-08-20）

クライアントが最初の参考写真の花を気に入っていたため、花序を主要モチーフとして復帰。あわせて「ヨモギ横が不自然」の指摘に対処した。

- [x] **見出し行の終端をヨモギ→花序の標へ**。ヨモギの枝は「何かから生えている」形なので、点線ルールの先に浮いていると生育の文脈がなく不自然だった。花序は丸く完結した形なので行末の飾りとして無理なく納まる。茎は箱の左端・中央高さから入れ、点線がそのまま茎に続いて見えるようにした
- [x] **株に花を戻した**。viewBox を 460×420 → 460×560 に伸ばして天地を確保し、葉の上へ花枝が抜ける（写真どおりの構造）
- [x] 白い小花はページ地とのコントラストが **1.08** しかなく単独では消える（実測）。写真同様に蕾（淡い黄緑 `--bloom-bud #CBDF9B`）を混ぜ、小花に淡い緑の輪郭を入れて視認性を確保。蕾の輪郭は茎より一段深い緑にして小花の形が出るようにした
- [x] ヒーローは「葉＋花枝」ではなく**株**に変更。単独の花枝は背景を選ぶが、株なら花が必ず自分の葉に載るので構造的に破綻しない
- [x] **可読性の実害を修正**: 植物を大きくした結果、`.mock-conv-list li` の裏に葉が来て本文が **3.26:1** まで低下していた（基準割れ）。カード背景を不透過（`#E7EDF3`）にして **6.87:1** へ回復。`.mock-fac-room` も同様に不透過化。いずれも chambray-gyoen スコープのみ

**既知の注意点**（次に触る人向け）:
- 装飾レイヤーは本文の下（z-index:0）に敷く方針。ただし**本文側の背景が半透明だと透ける**ので、植物が届きうる範囲のカードは不透過にすること
- フッターはページ最後の要素なので `view()` の進捗を最後まで消化できず、視差が +40px ほど残る。`footer .fl { --drift:18px }` で弱め、基準位置もその分見込んである
- `footer` / `.ft` / `.ft-bot` に `position:relative` と `z-index` を付与（装飾レイヤーの基準＋コンテンツを植物より前に出すため）
- three.js 追加のため `pnpm-workspace.yaml`（allowBuilds）を新規作成。pnpm 11 はこれがないと `ERR_PNPM_IGNORED_BUILDS` で install が落ちる
- `pnpm-lock.yaml` は lockfileVersion 9.0 のまま。差分は three/@types/three の追加と、pnpm 11 が付与する `libc:` フィールド等
- ブラウザ自動化のタブは `document.hidden` が常に true を返すため、木漏れ日の目視確認には `--enable-unsafe-swiftshader --use-angle=swiftshader` 付きの headless Chrome を使うこと

### washed-chambray の木を chambray-gyoen と同じ明るさに（2026-08-20）

2案を並べたとき、木だけが washed-chambray 側で一段暗く（ウォルナット L*23〜47）、
chambray-gyoen（淡いオーク L*60〜86）と揃っていなかった。`app/chambray-design.css` の
WOOD トークンのみを差し替えて明度を完全に一致させた。色相は washed-chambray 側の
赤みを残しているので、2案の木は「同じ明るさの別の樹種」に見える。

- [x] `--wood #CE9F77` / `--wood-dk #B68762` / `--wood-lt #DEB791` / `--wood-pale #EED5B5`
      （L* は gyoen の 69.0 / 60.0 / 76.9 / 86.4 と同値）
- [x] `--wood-ink #6B4A32` を新設。**木の色で明るい地に文字を置くときの唯一の色**
      （淡いオークのままだと本文サイズで 2:1 前後まで落ちる）。`.h1-sub` `.c-head .kick`
      `.c-head h2 em` `.rhead h2 em` `.linfo h2 em` をこれに寄せ、見た目は従来どおり
- [x] `.mat-wood` は地が明るくなったので上に載る文字を `--ecru` → `--shadow`（6.32:1）へ。
      木目テクスチャの feColorMatrix も地より暗く出るよう 0.55/0.40/0.27 に
- [x] ヒーロー下のファクトレール（`.rail`）は淡いオーク地になるため、文字を `--shadow`、
      罫線を `rgba(43,38,32,.22)`、SCROLL の標を `--indigo`（4.95:1）に。
      ※ chambray-gyoen 側の同じレールは ecru のままで 2.06:1 しかない（既存の未修正箇所）
- [x] `.acc .ln.w`（アクセス行のリング）は淡すぎて明るい地に消えるため `--wood-dk` に
- [x] 検証: build 24/24・lint エラー0（警告は既存8件）、1440px と 375px で目視確認、
      chambray-gyoen は無変更
- 未着手（要判断）: ①`scenes.ts` のイラスト内部の木は両テーマ共用のためウォルナットのまま
  （＝2案間の差はない）。②`globals.css` の `--color-accent2 #6b4a32`（下層ページ用の無垢材
  アクセント／プレビュー用スウォッチ）は、文字色と cream 抜き文字の両方に使われていて
  そのまま明るくするとコントラストが落ちるため据え置き

### washed-chambray の緑を chambray-gyoen の植物色レンジへ（2026-08-20）

「緑の雰囲気も chambray green の植物の色にして明るくしたい」への対応。イラストの緑は
`scenes.ts` にハードコードされていて **washed-chambray と chambray-gyoen の共有アセット**
だったため、まず緑だけをテーマ変数に逃がしてから washed-chambray 側の値を差し替えた。

- [x] `scenes.ts` の緑20色（計61箇所）を `var(--sc-…)` 参照に変更。**旧HEX 1つ = トークン 1つ**
      の1:1対応で統合していない（統合すると chambray-gyoen の見た目が動くため）
- [x] `chambray-gyoen-design.css` に同名トークンを**元のHEXそのまま**で定義 →
      chambray-gyoen は完全に無変更（ブラウザで20トークン全一致を確認）
- [x] `chambray-design.css` の `--sc-…` は明るい御苑グリーンに。旧 L*22〜78 を
      **L*51〜80 に圧縮**（下限は gyoen の「暗色を入れない」下限 L*51、上限は
      イラスト内の空 L*84 より暗く保つため 80 で止めた）。彩度・色相は gyoen の
      植物色に合わせて C≈46 / h≈124、植え込みだけ C≈26 の抑えたグループとして分離
- [x] `--forest` 系4色は gyoen と**同値**に（`#5F8437` / `#83A94D` / `#A8C765` / `#C9DC8E`）。
      `--forest-deep` を新設し、明るい地に置くリング（`.acc .ln.g`）はこれを使う
- [x] `--forest-ink #3E5C28` は据え置き（明るい地に置ける唯一の緑の文字色・5.11:1）
- [x] `.mat-forest` は面が明るくなったので上に載る文字を `--ecru`（2.36:1）→ `--shadow`（5.53:1）、
      `.c-swatch .cap` も同様
- [x] 検証: build 24/24・lint エラー0、緑の文字は全3箇所とも 5.11:1、
      1440px / 375px で目視確認（375px 横スクロール0）、chambray-gyoen 無変更
- 未対応: chambray-gyoen 側のイラストの緑は濃緑のまま（＝今は washed-chambray の方が明るい）。
  揃えるなら `chambray-gyoen-design.css` の `--sc-…` ブロックを washed-chambray の値に
  差し替えるだけでよい

---

### Chambray Green 植物のマテリアル改訂 — Step 1（2026-08-21）

「もっと lush / moisture-rich（生き生きとした、水分を含んだ有機的な空間）にしたい」への対応。

**現状分析で分かったこと**：植物が「切り抜き素材」に見えるのは配置だけの問題ではなかった。
計測すると flora パレットは **L\*60.9–86.8（26単位）/ 色相幅 9.6° / 彩度52–71で一定**。
これは物理的にビニール・カッティングシートの見え方で、どう並べ替えても濡れた葉にはならない。
さらに同じ `defs.ts` の `cg-canopy`（ヒーロー背景）は遠景 L\*76.1 op.55+blur → 中景 L\*64.8
→ 前景 L\*50.9+blur と**正しい奥行きモデルを既に持っていた**。つまり1ページに2つの異なる
マテリアルモデルが同居していた。flora を canopy 側の光のモデルに合わせるのが Step 1。

- [x] **明度ラダーを 14単位 → 62単位（L\*34–96）へ**。`--leaf-shade #2F5936` / `--leaf-core #46793E`
      を新設、`--leaf-dk` を L\*60.9 → 57.0 に。暗部は「葉の重なりの内側」だけに使う
- [x] **色相を 9.6° → 43°（h103–146）へ**。明るいほど黄（`--leaf-new #BCCC68` h113）、
      暗いほど青緑（`--leaf-shade` h146）。影が青緑に寄るのは影が空の色を拾うからで、
      この地（h253 の青）がそのまま環境光になる。**湿度の正体はこの色相回転**
- [x] **彩度を明度に連動**（C\*62→40→28）。日陰は彩度が落ちる。全域で高彩度＝乾いたポスターカラー
- [x] **鏡面ハイライト `--leaf-wet #E3F7F9` を新設**。ツヤと濡れを分けるのはエッジの硬さなので、
      ぼかさず・小さく（葉身の2〜3%）置く。アオキの旧ハイライトは `stroke-width:4 / opacity:.5`
      の柔らかい線でサテンに見えていた → 不透明の紡錘形に置換
- [x] アオキを陰側 `--aoki L*55` / 受光側 `--aoki-hi L*72` の2面に分割（1枚の葉に17単位の明度差）
- [x] 実に陰側 `--berry-dk` を敷き、明部を右上へずらして球にした。鏡面は葉より硬く（不透明）
- [x] ヤツデ株の奥行きを3段→4段に。葉身だけでなく**葉脈も一緒に落とす**（奥＝脈が沈んだ塊、
      手前＝脈まで見える葉 ＝「距離」として読める）。最前列のみ `--leaf-new` で新芽
- [x] ヨモギの奥1枚を `--mug-sh L*61` に。3枚が同値だと重なりが読めず1枚の記号に潰れる
- [x] 斑入りアオキにも同じ鏡面を通す（ここだけ光沢がないと別セットの素材に見える）
- [x] レンガ地（`.mat-shadow .fl`）は**ラダー全段を +12L\* 平行移動**。1段だけ持ち上げると
      ラダーが潰れてこの節だけ平板な葉に戻る。色相の回転は地が変わっても同じなので維持
- [x] `npm run build` 通過 / lint エラー0 / コンソールエラー0 / 1200px・375px で目視確認

**「暗色を入れない」規則の解釈**（2026-08-21 確認済み）：
この規則は**ページのトーン（地・大きな面・文字）を守るためのもの**で、イラスト内部の陰影
モデリングは対象外。暗部の面積はページ全体の **0.54%**、1画面あたりでも **1.7%** なので
ページのトーンは動かない。地・面・文字には引き続き暗色を使わない。

**実装上の注意（ハマりどころ）**：
- 葉の陰を `radialGradient` で葉柄の付け根に置いたら、裂片が細長いヤツデでは
  「中心が暗く先端が明るい」＝**アガベ／ユッカの見え方**になった。方向性のある
  `linearGradient`（光源側＝右上が明るい）に替えて解決。光源は `cg-canopy` の
  `cg-sunG`（cx=.72 cy=.2）と揃えてある
- **paint server の stop はシャドウツリー越しのカスタムプロパティを見ない**。
  `<use>` で参照されるシンボル内の `fill="var(--x)"` は効くが、`<linearGradient>` の
  `stop-color="var(--x)"` は `.mat-shadow .fl` の上書きを拾わない。よって stop は
  ハードコードし、レンガ地用の明るい版を別定義して、**継承するカスタムプロパティ
  `--shade-paint`（値が `url(#…)`）で切り替える**

**未着手（Step 2 以降）**：
- Step 2 = 配置の再設計。18個の単体を6〜7の群落へ。全ページで**重なっている植物のペアが0**、
  全個体が画面端200px以内にL/R交互＝ページ罫飾りの構造、という配置側の問題は未解決
- `.flora { overflow:hidden }` によるセクション境界の定規カット（茎が宙で切れる）
- `ChambrayGyoenTop.tsx:142` の viewBox 不一致（`0 0 460 420` vs シンボルの `0 0 460 560`）。
  最大の株が意図の **75%** でしか描画されていない（実測: 箱410×499 に対しインク304×356）
- ヤツデのシンボル形状そのものが棘っぽい（裂片が細長く谷が深い）。Step 3 で描き起こし

### Chambray Green 植物の配置再設計 — Step 2（2026-08-21）

Step 1（マテリアル）に続く配置側の改訂。**単体18個 → 群落7つ**へ。

**Step 1 の時点で残っていた計測値**：
- 全ページで**重なっている植物のペアが 0**。完結した単体シルエットは脳が確実に
  「切り抜き素材」と判定する
- 大株2つを除く全個体が画面端 200px 以内、しかも上から L/R ほぼ交互＝便箋の縁飾りの構造
- 実インク幅が leaf 152 / avar 157 / aucuba 147 / mug 136 と、下位4種が全部同じ大きさ。
  宣言していた強/中/弱の3段が視覚的に存在しなかった

- [x] **群落システムを導入**。`<i class="cl">` の中に `.slot`（位置と幅は % のインライン）
      で `.fl` を入れ子にする。中の要素は必ず 25〜40% 食い込ませ、`z-back` / `z-front` の
      層に振り分ける（**葉身だけでなく葉脈も一緒に落とす**のが要点）
- [x] 群落内の重なりペア **24/32**（改訂前はページ全体で 0）
- [x] **主群落2（ヒーロー・アクセス）／副群落2（立地・部屋）／掛け3（リノベ・設備・フッター）**
      の7つに再編。主群落は2種類のレシピを使い分ける（ANCHOR＝ヤツデ主導、
      ANCHOR2＝アオキとヨモギ主導）。同じレシピを2回使うと「型」が見える
- [x] 各群落は外周へ葉を1枚「飛ばす」。閉じた輪郭を持つ塊は、中身がどれだけ複雑でも
      1個のオブジェクトとして読まれ、「ただの大きなイラスト」になる
- [x] **`ChambrayGyoenTop.tsx` の viewBox 不一致を修正**（`0 0 460 420` → `0 0 460 560`）。
      株のインクが **304×356 → 433×504**、面積充填率 **0.53 → 0.98**（面積で2.0倍）
- [x] 視差（`cg-drift`）を `.flora > .fl, .flora > .cl` に限定。群落の中の1枚ずつを
      別々に動かすと、まとまりがほどけて元の「バラバラの素材」に戻る。群れは1つの物体として動く
- [x] `npm run build` 通過 / lint エラー0 / コンソールエラー0 / 横スクロール0（1280・375）

**接地の規則（「浮き」を消すために確立）**：
群落の足元は必ず何かに隠す。開いた地の上で終わると、どれだけ大きく組んでも宙に浮く。
隠し先は2種類だけ——
- (a) **不透明なコンテンツ**に潜らせる：ヒーローの木レール、部屋の帯、設備カード、
      リノベのサンプルパネル
- (b) **地の色が変わる境界**の下へ送る：立地→レンガ、アクセス→フッター、ページ末尾

逆に、**境界の前後で地の色が同じ場所（設備→部屋、部屋→アクセス）では絶対に跨がせない**。
何にも遮られずに水平の直線で切れるので、「定規で切った」ように見える。改訂前の
`bottom:-2%` 〜 `-7%` がまさにこれだった。左右は逆で、画面端で切れたものは
「フレームの外へ続いている」と読めるので、**全群落を左右いずれかの端で切っている**。

**視線設計**：入口（ヒーロー・強）→ 立地下端（中）→ リノベ（点）→ 設備（点）→
部屋下端（中）→ 出口（アクセス・強）。読んでいる最中に横から呼びかけないよう、
長い節では下端にだけ置く。無植物のスクロール比は **約41%**（立地の 1765px と
部屋中央の 1940px）。余白はその両側に密度があるときだけ余白になる。

**モバイル（375px）で別途取り直した点**：
足元を隠す相手が狭い幅では縦に伸びるため、デスクトップの % 指定のままだと隠れすぎる。
- ヒーローのレールが 57px → **220px**（下端から18%）に伸び、`bottom:2%` では群落が
  丸ごとレールの裏に消えていた → `max-width:520px` で `bottom:15%`
- 部屋の3枚目の帯が群落の上8割を覆っていた → `bottom:1.5%` かつ `--drift:30px`
  （視差の分だけ部屋→アクセス境界にはみ出して、そこで切れるのを防ぐ）

**本文との干渉チェック**：本文要素×植物インクの矩形重なりを全ページで走査。
検出された6件はすべて**不透明な面の裏**（ヒーローの `.patch` は `rgb(58,94,125)`、
設備の `.mock-fac-room` は `rgb(242,239,232)`）で、可読性への実害なし。
設備の掛けは当初 `top:50%` で見出しと本文に掛かっていたので `top:62%` へ下げ、
カードの裏に回した。

**未着手（Step 3）**：
- ヤツデのシンボル形状。裂片が細長く谷が深いため、株を 1.33 倍にしたことで
  「アガベ／ユッカ」に見える傾向がむしろ強まった。参考写真のヤツデは裂片がもっと
  幅広い。ここは描き起こしでしか直らない
- 被覆率（Lush の最後の10%）。現状は主群落の画面でもインク比が控えめ

### Chambray Green 植物の形状描き直しと被覆率 — Step 3（2026-08-21）

Step 2 の残件2つ（ヤツデの形状／Lush の最後の10%＝被覆率）に対応。

**ヤツデの裂片を描き直した（最大の変更）**

改訂前の裂片は **最大半幅/長さ = 21/130 = 0.162**、しかも最大幅の位置が長さの **54%** と
外寄りだった。この2つが重なると裂片は「細い棘」になり、7枚を放射させたときに中心付近へ
大きな空きくさびが7つできる。結果として掌状葉ではなく**アガベ／ユッカ**に見えていた。
Step 2 で株を 1.33 倍にしたことで、この傾向はむしろ強まっていた。

- [x] 新しい輪郭は **25.2/130 = 0.194**、最大幅の位置を **42%** へ内寄せ。
      **基部を太くすることが要点**で、隣り合う裂片は半径85（長さの65%）まで接して
      ひと続きの葉身になり、そこから先だけが分かれて尖る
- [x] 切れ込みの深さ **35%**（改訂前は中心の1点に収束＝実質100%）。実物のヤツデの見え方
- [x] 裂片1枚の面積 **2900 → 4358 単位²（+50%）**。被覆率もここで半分稼いでいる
- [x] 葉脈の終点を新しい裂片長に合わせ、鏡面を裂片の中心線から外へずらした
      （主脈と重なると1本の光る筋に見え、葉の面ではなく線に読める）

設計上のトレードオフ：7裂片＝26°間隔なので、隣と接する境界線は `w = r·tan13°`。
`w(r)` がこの線を下から横切ると穴が開き、上から横切ると正常な切れ込みになる。
**深い切れ込みと太い裂片は両立しない**ので、切れ込み35%・最大半幅0.194 が
「星に見えない」ぎりぎりの均衡点。倒卵形（上部で最大幅）も試したが、
裂片が上で重なって中心に閉じた穴が開く（モンステラの見え方）ため却下。

**被覆率＝Lush の最後の10%**

- [x] **遠景層 `--leaf-air #B7DBB1`（L\*84 C\*26 h140）と `.z-air` を追加。**
      彩度を 62 → 26 まで落とし、葉脈を葉身と同色にして「輪郭のぼやけた塊」にする。
      `cg-canopy` の遠景（L\*76.1 / opacity .55 / blur）と同じ役目を群落の中で果たす
- [x] 主群落を5員 → **7員**、副群落を4員 → 5員へ。増やした分はすべて遠景層
- [x] 遠景層では `--leaf-wet:transparent`。奥の葉に鏡面が乗ると、その1枚だけ手前へ
      飛び出して層が壊れる

**Lush は密度の問題なので面積は要るが、全部を前景の彩度で描くと密度がそのまま
うるささになる。奥を薄く敷いて手前を濃くする＝局所的に濃く、全体としては軽い。**

**繰り返しの露見に対処**

裂片を太くして株を大きくした結果、**全部の葉で葉脈と鏡面の位置がそろっている**ことが
はっきり見えるようになった（型紙で抜いたように見える）。

- [x] 鏡面の不透明度を **.9 → .58**、寸法も一段小さく。デカールではなく「照り」にする。
      アオキ側も 1.0 → .72（1枝に同一の葉が4枚並ぶので特に目立っていた）
- [x] 群落の各員に **傾き（±16°まで）と左右反転**を振った。`Member` に `rot` / `flip`
- [x] **`.fl.flip` は無効だった**ので `.slot` 側へ移動。内側の `.fl > span` は
      `cg-sway` が transform を使い切っているため、そちらに書いても効かない。
      あわせて `.slot` に `transform-origin:50% 100%`（葉柄の位置で傾く）
- [x] 揺れの位相を7員ぶんに拡張。周期は互いに素に近い値（公約数があると数十秒ごとに
      全員の位相がそろい、群落がひとかたまりで振れる瞬間ができる）

- [x] `npm run build` 通過 / lint エラー0 / コンソールエラー0 / 横スクロール0（1280・375）

**Step 1〜3 の到達点**：Organic / Alive / Layered / Cohesive / Botanical / Atmospheric /
Moisture-rich は達成。**Lush は被覆率の上限がクライアント方針（重くなりすぎない）と
正面から衝突するため、意図的にここで打ち止め**にしている。上げるとしたら群落数ではなく
遠景層の面積を増やす方向（視覚的な重さを増やさずに密度だけ上がるため）。

### 次の作業 — モバイル修正の引き継ぎ（2026-08-21 時点）

Step 1〜3（植物のマテリアル／配置／形状）は完了。次はモバイルに着手する。
**以下は植物レイヤーを触る過程で観測した範囲のメモであり、モバイル全体の
監査はまだ行っていない。**着手時はまず 375px で通しで見ること。

**計測済みの数値（375px / 1280px）**

| | 375px | 1280px |
|---|---|---|
| ページ高 | 10,959px | 8,931px |
| ヒーロー | 0–1,219（`100vh`） | 0–900 |
| 立地 | 1,219–3,593 | 900–2,935 |
| リノベ | 3,593–4,804 | 2,935–3,916 |
| 設備 | 4,804–6,588 | 3,916–5,247 |
| 部屋 | 6,588–8,722 | 5,247–7,592 |
| アクセス | 8,722–10,280 | 7,592–8,593 |
| フッター | 10,280–10,959 | 8,593–8,931 |
| ヒーローの木レール | 220px（下端から18%） | 57px（同6.3%） |
| 部屋の帯3枚 | 7037–7558 / 7559–8080 / 8081–8602 | — |

**既知の課題（植物まわり）**
- 植物のない区間が最大 **2,277px＝2.8画面**（立地）。デスクトップの
  1,765px は「呼吸」として設計したが、モバイルでは長すぎる可能性がある
- `.fl.ro-t`（部屋の見出し脇）は**何にも接していない唯一の要素**。
  デスクトップでも同じ。接地の規則から外れている
- モバイル専用の上書きは `@media (max-width:520px)` に2件だけ
  （`.cl.cl-he{bottom:15%}` / `.cl.cl-ro{bottom:1.5%;--drift:30px}`）。
  足元を隠す相手（レール・帯・カード）は幅で高さが変わるので、
  **他の群落も同じ理由で取り直しが要るかもしれない**（未確認）
- ブレークポイントは 1100 / 880 / 600 / 520px。520 と 375 の間は未検証

**確認済みで問題なかった点（375px）**
- 横スクロールなし / コンソールエラーなし
- 全群落が画面の左右いずれかの端で切れている
- 本文と植物の重なりは、すべて不透明な面の裏

**検証の手順（毎回これで確認している）**
- `?theme=chambray-gyoen` でテーマ切替。dev サーバーは `.claude/launch.json` の `k3v-dev`
- **フルページのスクリーンショットは使えない。** ヒーローが `100vh` なので、
  CDP がキャプチャ用にビューポートを全高へ広げた瞬間にレイアウトが壊れる。
  スクロール位置ごとのビューポート撮影で確認すること
- reveal は `document.querySelectorAll('.rv').forEach(e=>e.classList.add('in'))` で強制表示
- 要素の座標を測るときは `scrollTo` と `getBoundingClientRect()` を同じ式に混ぜないこと
  （`r.top` は旧スクロール、`scrollY` は新スクロールを読んで数値が壊れる）

### Chambray Green 植物の「見えている率」改善 — Step 4（2026-08-21）

「せっかくの植物イラストが本文にかぶって勿体無い」への対応。Step 2 で作った
接地の規則に**遮蔽量の上限がなかった**のが原因だった。

**計測方法**（`elementFromPoint` は透明な `.wrap` を拾うので使えない）：
インクの外接矩形を格子状に標本化し、各点が (a) 画面外／セクション境界の外、
(b) 不透明要素（背景 alpha≧0.9・画像・背景画像）の矩形の中、(c) それ以外＝見える、
のどれかを幾何計算で判定する。**各要素をビューポート中央までスクロールしてから測る**
こと——視差（`animation-timeline: view()`）は画面外だと進捗が 0/1 に張り付くので、
一箇所から測ると負の `bottom` を持つ要素の値が実態より良く出る。

**改訂前（1512px）**

| 群落 | 見える | 主因 |
|---|---|---|
| ヒーロー主群落 | **31%** | `.patch` の裏 25% |
| 部屋 副群落 | **30%** | 部屋写真の裏 37% |
| 設備 掛け | **39%** | 設備カードの裏 36% |
| 部屋 見出し脇 | 100% | 遮蔽 0%＝接地していない |
| アクセス主群落 | 90% | — |

同じ設計から 31% と 90% が出ていた。規則に量が入っていなかった。

- [x] **遮蔽の予算を規則に追加**：見えている 65〜80% ／ 隠れ 20〜35%。
      50% を超えて隠れているものは接地ではなく**埋没**。画面端で失う分も
      「隠れ」に数える
- [x] ヒーロー主群落を左下からコピー塊とイラスト枠のあいだの空きへ移動。
      31% → **86%**（1512）/ **73%**（1280）
- [x] 設備の掛けを画面外へ出すのをやめ、カード左下角に3割だけ掛ける。39% → **71%**
- [x] 部屋の副群落を一回り小さくして下げ、帯の裏に沈む量を3割前後に。30% → **65%**
- [x] アクセス主群落を縮小して下げた（1280 でチェックイン行に **51%** かぶっていた）
- [x] 素の本文（不透明な面に載っていないテキスト）との重なり **0件**（1280 / 1512）
- [x] 平均 見えている率：**1280 で 64% / 1512 で 68%**
- [x] `npm run build` 通過 / lint エラー0 / 横スクロール0

**見送った案（重要）**
- 部屋の見出し脇に小さな株を置く案は**却下**。1512px では綺麗に納まるが、
  1280px では見出しの折り返しが1行増えて帯が下がり、株が帯に届かず宙に浮いた上、
  本文にも 6% かぶった。**セクション上端からの % では、テキストの折り返しで
  位置が動く要素に接地させられない。** 復活させるなら `.flora` ではなく帯そのものの
  中に入れて、帯を基準に位置を決めること
- ヒーローの足元を木レールに差し込む案も却下。レール高が 1280px で103px、
  1512px で57px と倍近く変わる（walk-time 行の折り返し）ため、単一の % では
  「浮く」か「沈む」かにしかならない。接地はイラスト枠の側面に預けた

**幅で変わるものに % で合わせない**、というのがこの Step の教訓。ヒーローは
「部屋イラストの左端はどの幅でも vw の約52%」という安定した基準を見つけて、
そこから `right:44%` で効かせている。

**ロケール横断で検証すること（Step 4 の追加分）**
`en` だけで詰めたあと `ja` で本文かぶりが再発した。日本語は文字が詰まるぶん
アクセス節の情報行の位置が変わり、`cl-ac` の上端が「チェックイン15:00〜」の行に
**13%** かかっていた。`.cl-ac` を一回り縮めて下へ送って解消（足元はフッター＝
地の色が変わる境界の下なので、下切れは接地として効く）。

**行の高さはロケールで変わる。テキストのすぐ下に群落の上端を置く設計は避けること。**

3ロケール × 1280px の最終値：平均 見えている率 **ja 65% / en 65% / zh 65%**、
横スクロール 0、素の本文との重なりは zh の `am-a ← 「智能马桶盖」2%`（チップ
ラベルの端に数px）のみで実害なし。build 通過 / lint エラー0 / dev ログのエラー0。

**未対応（次のモバイル作業で必須）**
375px で測ると平均 **42%**、内訳は ヒーロー **6%** / レンガ掛け **7%** /
設備掛け 27% / 立地 39% / 部屋 42% / アクセス 72% / フッター 100%。
モバイルは1カラムで本文が幅いっぱいに広がり、デスクトップにあった「脇の空き」が
消えるため、**配置を共有せず別に組む必要がある**。素の本文との重なりは 0 件なので
可読性の実害は今のところ無い。

### 別件：washed-chambray Hero を初期生成版に復元、ROOM 01 を2枚スライダー化（2026-08-21・確定）

クライアント指示で `HERO` を初期生成版（`1f3ea55` 時点、「朝の縁側」の情景）に
戻し、2026-08-17〜21 に `HERO` として使われていた実レイアウト版のイラストは
`scenes.ts` に `ROOM1_MORNING` として退避、ROOM 01 の2枚目スライドに採用した。

（一度この作業がビルドを壊しかけた経緯: `ROOM1_MORNING` への参照だけが先に
入り、シーン本体が無い状態でコミットされたことがあった。原因は解消済み）

- [x] `scenes.ts` の `HERO` を初期生成版に復元、`ROOM1_MORNING`（旧・実レイアウト版
      イラスト）を新規エクスポートとして追加
- [x] `ChambrayTop.tsx` / `ChambrayGyoenTop.tsx` の ROOM 01 `.pic` を2枚スライダー化
      （ラジオ入力 + CSS `:has()`、JS 不要。1枚目=`ROOM1`、2枚目=`ROOM1_MORNING`）
- [x] スライダー用 CSS（`.s-radio` / `.pic .ph.s0` / `.pic .ph.s1` / `.s-dots`）を
      `chambray-design.css` と `chambray-gyoen-design.css` 双方に実装
- [x] `docs/gen-chambray-assets.py` に警告コメントを追加（再実行すると
      `ROOM1_MORNING` とスライダー CSS が消え `HERO` も戻ってしまうため）
- [x] `CLAUDE.md` に本仕様を明文化（「washed-chambray Hero / ROOM 01 イラスト仕様」節）
      — 他セッションが理由を確認せず元に戻すのを防ぐため

### Chambray Green 植物のモバイル配置 — Step 5（2026-08-21）

375px でデスクトップの配置を流用すると見えている率が平均 42%（ヒーロー6% /
レンガ7%）しかなかった件への対応。**寸法の問題ではなく構成の問題**だった。

**モバイルの実測（375px / ja）**
- `.wrap` が **0–375 の全幅**。横方向の余白がゼロで、デスクトップの
  「左右端から生やして画面外へ逃がす」という接地の手が使えない
- 本文・カードを占有として計算すると、**縦の完全な空き帯が 35〜110px しかない**。
  1カラムに積み上がるぶん、群落を置ける面が残らない

**構成の変更**：モバイルは**低く横に広い帯**にして、足元を「すぐ下の不透明
ブロック」か「地の色が変わる境界」へ差し込む。壁の際に生えている草の見え方で、
狭い帯を素直に使える。実測で確認した受け皿：

| 節 | 空き帯 | 足元の受け皿 |
|---|---|---|
| ヒーロー | 597–677 (80px) | `.frame`（部屋イラストの木枠）y677 |
| 立地 | 3523–3593 (70px) | レンガ節 y3593（地の色が変わる） |
| 部屋 | 8642–8722 (80px) | 3枚目の帯（上から隠す＝写真の足元） |
| アクセス | 10234–10280 (46px) | フッター y10280（cream に変わる） |

- [x] `@media (max-width:700px)` に**モバイル専用の配置ブロック**を新設
- [x] **リノベーション（レンガ）と設備はモバイルでは出さない**。どちらも
      カードが縦に詰まり 35〜54px しか残らず、差し込んでも9割が裏に回る。
      全部の節に置くことより、出したものが見えていることを優先
- [x] 左右は上から **L→L→R→R**（アクセスを左へ回すと CTA と挿絵キャプションの
      裏に入るので、崩すのは立地側で行っている）
- [x] 部屋は当初1枚目の帯の上（空き35px）に置いたが、そこに収まる寸法まで
      詰めると 375px で75px になりほぼ見えなくなったので、**3枚目の帯の足元**へ移動
- [x] `npm run build` 通過 / lint エラー0 / 横スクロール0 / 3ロケール 200

**効かなかった手（記録）**
- `.cl { aspect-ratio }` を寝かせて「幅広・低い株」にする案は**無効**。
  中の要素は % で位置だけをもらい、高さは自分の縦横比から決まるので、
  箱を低くしても要素はそのまま下へはみ出す（箱117px に対しインク286px）。
  **インクの高さは実質「群落の幅」で決まる**ので、寸法は幅で合わせるしかない。

**下端に寄せるものは % ではなく px で指定する**
合わせたい相手が「節の下端そのもの」なのだから、節の高さに比例させる理由がない。
% のままだと文章量の違うロケールで節の高さが変わり、en では立地が 55% 裏に沈み、
アクセスは遮蔽ゼロで浮いた。

**計測の限界（重要）**
見えている率の計測は**インクの外接矩形**を格子で標本化しているので、
透明な隙間も「見えている」に数える粗い近似でしかない。実際、指標が
「75% 見えている」と出しているのに画面上は数ピクセルしか出ていない、
という食い違いを2回確認した（立地・ヒーロー）。
**大きな取りこぼしの検出には使えるが、詰めの調整は必ず目視で行うこと。**

**確認済み**：ヒーロー / 立地 / 部屋 / アクセス / フッターの5箇所を
ja・en の両方でスクリーンショット確認済み（部屋のみ ja）。zh は未目視。

**未対応**
- zh のモバイル目視確認
- 520〜700px（大きめスマホ〜小型タブレット）の中間幅は未検証

### モバイル：ヒーローの置き場変更とツタのガーランド追加 — Step 6（2026-08-21）

クライアント指摘2件への対応。

**① ヒーローの植物が部屋イラストに完全に隠れていた**
Step 5 では「木枠の上の空き帯」に置いていたが、この空きは 375px で80px、
**430px では42px** しかなく、幅が広がるほど枠に飲まれる（430px 実測で
株 623–777 に対し枠が 637 から始まり、ほぼ全部が裏）。

- [x] クライアント提案どおり**木レールの右下**へ移した。レールは徒歩時間が
      3段に積まれた帯で、3行目・4行目の文字は x254 / x106 までしか来ず、
      右側 170×104px がまるごと空いている（430px 実測）
- [x] レールは **z-index:3 の不透明な木の面**なので、ヒーローの `.flora` だけ
      z-index:4 へ出した。木の帯に鉢が載っている見え方になり、足元は
      ヒーロー下端（木→淡い地の変わり目）で切れて接地する
- [x] **木の地に載る株だけ配色を差し替え**。木レールはオレンジの暖色で、
      淡い緑（`--leaf-air` の彩度26）は彩度で負けて灰色に見えた。この株は
      遠景層を使わず、奥行きを明度だけで取る

**② 植物の印象が弱い → ツタのガーランドを追加（モバイル専用）**

モバイルに無いのは**面**であって、**縁**は大量にある。細く長い蔓なら縁に
沿わせるだけで長さを稼げ、面積を要求しない。

- [x] `cg-ivy-leaf` / `cg-ivy-garland` を `flora.ts` に新設。葉は5裂だが
      裂けは浅く（半径42/62＝68%まで隣と接する）、基部が広い＝アイビーの見え方。
      素材は既存と同じ（明度ラダー・色相回転・硬い鏡面）なので、別のセットから
      持ってきたようには見えない
- [x] **レンガ帯の上端と下端**に掛ける。上下どちらも地の色が変わる境界で、
      蔓を掛ける相手としては全ページで最も強い。上＝壁を越えてくる蔓、
      下＝壁から出てくる蔓（`scaleY(-1)` で反転）
- [x] これで Step 5 で「受け皿が無い」として植物を消していた**リノベーションと
      設備に緑が戻った**
- [x] 幅は 118vw で左右へ抜けさせる（縁飾りではなく「続いているもの」に見せる）
- [x] `npm run build` 通過 / lint エラー0 / 横スクロール0 / 3ロケール 200 /
      デスクトップは無影響（`@media (max-width:700px)` 内に閉じている。
      外に出るのは `.fl.gl {display:none}` のみ）

**種の一貫性について（要確認）**
ツタは参考資料4点（ヤツデ・ヨモギ・アオキ・斑入りアオキ）に無い**新しい種**。
クライアント指示「モバイルのみアイビー（蔦）ガーランド」に基づく追加だが、
「御苑の林床の3種」という当初の設計からは外れる。デスクトップには出していない。

**効かなかった手（記録）**
- ガーランドの奥の葉に `--leaf-air`（彩度26）を使うと、レンガの暖色の上では
  彩度が負けて**枯れ葉**に見えた。境界に掛ける前提の要素は、奥を淡くせず
  `--leaf-core`（暗い緑）で取る。**明度で退かせて彩度では退かせない**
- ヒーローの位置を `bottom: px` で固定したら 375px で丸ごと画面外へ落ちた
  （430px では見えていた）。群落のインクは箱から下へ大きくはみ出し、その量が
  幅に比例するので、**モバイルの位置と寸法は vw で持つこと**

**未対応**
- zh のモバイル目視（ja / en は確認済み）
- 430px は数値では確認したが、ブラウザペインが独自サイズでしか描画しないため
  目視は 375px で行っている

### 情報密度とタイポグラフィの整理 — 「Find the Door」を基準に全モックへ（2026-08-22）

クライアント所見：**Gyoen Green の「Find the Door」のメリハリが、スマホで
シンプルに伝わって非常に良い**。あそこを基準に、他の情報過多な箇所も同じ
考え方で整理してほしい。指示は「小さくしろ」ではなく **「情報を減らして、
重要な内容が一瞬で分かるように」**。

**基準（③仕様）として実測した値** — `#gy-access .mock-access-lead`
（`.jp.jp-lead`）：Zen Kaku Gothic New / **15.04px / weight 500 /
地のインク全濃度 / line-height 1.95 / letter-spacing 0.04em**。
① `h2.dsp`（18.4px / 800）→ ② その2行目 → ③ このリード、という3段。
**階層を色や大きさではなく「太さ」で取っている**のがこの並びの効いている理由。

#### ① 見出し下の3行を③仕様の横一行に（クライアント明示指示）
指示：「色の濃さ、字の大きさ・太さを③仕様にして横一行に収まるように」
（例：`▪ 住所　東京都新宿区新宿1-19-6`）

- [x] **文言を「事実そのもの」まで短縮**（`access.details`・3言語同時）
      - 住所 `〒160-0022 東京都新宿区新宿１丁目１９−６` → `東京都新宿区新宿1-19-6`
        （〒と「Japan」を含む完全形は各デザインのフッターに残っている）
      - 最寄り駅 `新宿御苑前駅・新宿駅 徒歩圏` → `新宿御苑前駅・新宿駅`
        （「徒歩圏」は直下のマップキャプションと重複）
      - `チェックイン / 15:00〜 / チェックアウト 11:00` → **`IN / OUT` / `15:00 / 11:00`**
        （ラベルが両方を担うので値から重複語が消える。zh は `入住 / 退房`）
- [x] **Gyoen Green**：チップ（丸ピル・11px・字送り0.14em）をやめ、
      `<dl class="cta-perks"> / .mock-fact` の情報行に置き換え。ラベル700 /
      値500、どちらも③と同じ級数・同じインク。行間に細いルール。
      → 375px で **住所が2行＋「住所」まで縦割れ** していたのが全行1行に
- [x] **Washed Chambray / Chambray Gyoen**：`ul.acc` は元から
      ラベル左・値右の1行組だったが、`@media (max-width:600px)` で
      `flex-direction:column` に落としていた（値が長文だったため）。
      値を短くしたのでこの縦積みを解除、1件75px → **49px**
- [x] **現行デザイン**：`Access.tsx` の `dl` を全幅で1行組に（`sm:flex-row` 解除）
- [x] 実測：ja / en / zh × 4デザイン × 320 / 375 / 1280px で
      **全行1行・横スクロール0**（例外は en × Gyoen Green × 320px のみ、
      住所と駅名が2行に折れる。「読みやすさ > 1行」の優先順位どおり許容）

#### ② 「夜中でも、ふらっと〜」の下（クライアント指示：短く、直感的に）
- [x] リード本文を短縮（ja 89字→51字 = 4行→3行、en/zh も同様）
- [x] 4枚のカード本文を**全て1行に収まる長さ**へ短縮（ja/en/zh）。
      見出し（太字）が主張、本文は1行の補足、という読み方に統一
- [x] `.mock-conv-lead` を **13px/行送り2.1 → 14px/1.95**、インクも一段濃く
      （3デザインとも）。「小さい＝簡潔」ではないので、短くした分だけ上げる

#### ③ 同じ考え方を全モックへ
- [x] **ヒーローのリード**（全4デザイン共通の `hero.description`）：
      ja 105字→63字。**モバイルで6行→3行**になり、eyebrow〜CTAが1画面に収まる。
      中央でラテン大文字が44字続く `UNICO・JOURNAL STANDARD FURNITURE・CRASH GATE`
      が2行分を占めて和文のリズムを切っていたのが density の主因。
      3ブランド名は ROOM セクションと `renovated.points` に残っている
- [x] **近隣ショップのチップ**（Gyoen Green `.mock-chiprow`）：11px・字送り0.14em の
      「札」の寸法 → 12px・字送り0.04em の和文寸法へ。値の不透明度 0.72→0.85。
      `.chip` 本体（UNICO / ROOM 01 などラテンの札）は触っていない
- [x] **マップキャプション**（Gyoen Green `.loc .mapnote`）：11px 等幅・字送り0.1em →
      12px 和文書体・インク0.9
- [x] **素材カードの説明文**（chambray 2案 `.sample p`）：11px / 不透明度0.62 →
      12.5px / 0.8。影の地の上で本文として細すぎた
- [x] **近隣2枚のキャプション**（chambray 2案 `.hoods .h .t`）：9px 等幅大文字 →
      11px 和文。絵の明るい側で文字が落ちるので `.hoods .h::after` に
      下端だけの薄い暗がりを追加（**イラストの色は1色も動かしていない**）
- [x] en の `mapCaption` が長すぎて2行になっていたので短縮

#### 意図的に触っていない箇所
- **「Find the Door」の①②③そのもの** — クライアントが良いと言った並び。基準側なので不変
- **ROOM 01〜03 の紹介文**（各4行）— 1部屋1段落。大きなイラストで分断されるので壁にならない
- **設備の注記** `amenities.note`（4行・13px）— セクション末尾の脚注。弱いのが正しい
- **`primeLocation.description`**（4行）— 基準③と同じ役割・同じ級数のセクションリード
- **Washed Chambray の縦組み** `hero-vjp`（12.48px・字送り0.34em）— 縦書きは
      このデザインの署名であって本文ではない
- **レイアウト・セクション順・配色・イラスト・写真** — 一切変更なし
- **`/rooms` `/stay` 等の下層ページ** — 今回のレビュー対象はTOPの4案

**確認済み**：`npm run build` 通過 / lint エラー0（既存warning 5件のまま）/
ja・en・zh × current・gyoen-green・washed-chambray・chambray-gyoen で
横スクロール0 / モバイル実画面で目視

**別件で見つけた既存バグ（未修正・本件と無関係）**：
`GyoenTop.tsx:396` が `gyoen.hero.stamp` を参照しているが3言語とも未定義
（存在するのは `chambray.hero.stamp`）。next-intl が毎レンダーで
MISSING_MESSAGE を出し、ROOM 01 スライダー2枚目の aria-label が
キー文字列そのままになっている。

## Phase 2 — 追加ページ（サイトマップ準拠）

要件定義書 10章のサイトマップに基づく。App Router で `app/[locale]/xxx/page.tsx` として追加し、Nav/Footer からリンクする。

- [x] `/rooms` — 3部屋一覧
  - [x] `/rooms/unico`（ROOM 01）
  - [x] `/rooms/journal-standard-furniture`（ROOM 02）
  - [x] `/rooms/crash-gate`（ROOM 03・詳細未定のため「準備中」表示で実装。家具搬入・撮影後に本文差し替え必須）
  - [x] `/rooms/meet-the-designer`（Meet the Designer — 家具選びのストーリー。要件定義書9章のトーン例を参照して実装）
- [x] `/stay`（Stay Plan / Book）
  - [x] 空室状況セクション — 3部屋へのリンク＋「オンライン予約システム準備中」の正直なプレースホルダー＋問い合わせCTA（外部予約エンジン未選定のため、現状はメール問い合わせ導線。Beds24/CHILLNW連携はPhase 3で確定後に差し替え）
  - [x] Direct Booking Benefits（`BookDirect.tsx` を `showCta` prop付きで再利用）/ Long Stay Plan / Family Stay / Business Stay の訴求
  - [x] Reservation（予約の流れ3ステップ＋問い合わせCTA。実際の決済・予約実行は予約エンジン連携後）
  - [x] サイト全体の「空室を探す」CTA（Nav・モバイルFAB・Hero・Access・BookDirect）を `/stay` への実リンクに統一。Footerのidは `#stay`→`#contact` に改名（問い合わせ専用であることを明確化）
- [ ] `/area` — 新宿御苑・新宿駅・周辺コンビニ/スーパー/飲食店・交通・観光・モデルコース・好立地訴求・リノベーション訴求
- [ ] `/gallery` — Room Photos / Lifestyle Photos / Neighborhood Photos / Videos
- [ ] `/reviews`
- [ ] `/news`
- [ ] `/faq`
- [ ] `/neighborhood-guide` — おすすめコーヒー/ベーカリー/朝食/レストラン/銭湯/古着店 等
- [ ] `/contact`

各ページ着手時は先に `messages/*.json` にセクションキーを追加してから実装する（3言語同時追従）。

---

## Phase 3 — 予約導線・機能要件

- [x] フローティング予約ボタンを全ページ共通化（`layout.tsx` に `Nav`/`MobileBottomNav` を組み込み済み。CTA遷移先も `/stay` に統一）
- [~] 各部屋ページから空室カレンダー・予約ボタンへ「トップに戻らせない」導線を設計 — 各部屋詳細ページのCTAは同一ページ内の `#contact`（フッター問い合わせ）に直接遷移する形で実装済み。ただし実際の空室カレンダーは予約エンジン未選定のため未実装
- [ ] Beds24 / CHILLNW 等の外部予約エンジンとの連携方式を確定（埋め込み/リンク/API）— クライアントに確認要。確定後、`/stay` の「空室状況・ご予約」セクションと各部屋ページCTAを実際のカレンダー/予約導線に差し替える
- [ ] TOPから3クリック以内で予約完了できるか動線をテスト（現状は「予約」ではなく「問い合わせ」で完結するため、実予約フローが確定してから再テスト）

---

## Phase 4 — 集客・SEO

- [ ] Googleビジネスプロフィール登録・公式サイトへのリンク設置（必須）
- [ ] 「新宿御苑 民泊」等のキーワードを軸にした構造化・メタデータ設計（各ページの `meta.title`/`meta.description`）
- [ ] Google宿泊広告（Google Travel）連携は要件定義書5章参照、実装可否をクライアントと確認

---

## 未確定事項（クライアントへの確認が必要）

- 正式な住所（丁目・番地）※最寄り駅の徒歩分数は要件定義書に記載あり、`PrimeLocation.tsx` に反映済み
- ROOM 03 CRASH GATE の詳細（要件定義書上も「未定・準備中」）
- 予約エンジン（Beds24 / CHILLNW）の最終選定
- Google宿泊広告を実施するか
- Guest Reviews の実データ（`Reviews.tsx` は開業直後のため「準備中」プレースホルダーで実装。実際の宿泊者レビューが集まり次第、差し替えが必要）
- **部屋番号 305 / 401 / 503 とブランド3室（UNICO / JOURNAL STANDARD FURNITURE / CRASH GATE）の対応**（Phase 1.5-A の設備訴求に必要。確定するまで部屋番号キーのまま実装）
- **ミュージック仕様の機器選定**（クライアント曰く「アイテムはこれから選定」。機種名は書かない）
- **新宿三丁目・歌舞伎町までの徒歩分数**（要件定義書に記載なし。Phase 1.5 では定性表現に留めた。実測が出れば数字で訴求できる）
- 各部屋の詳細スペック（面積・定員・ベッドタイプ）※要件定義書に記載がないため、`roomDetails`（messages/*.json）ではチェックイン/アウト時間と設備概要のみ掲載し、面積・定員は未掲載。実測値が出たらspecsに追加
